@model IEnumerable<Viajou.Models.Contato>

@{
    ViewData["Title"] = "Fale conosco";
}

<main class="vh-100 mt-4">
<h1>Registrar mensagens</h1>

<p>
    <a asp-action="Create">Nova mensagem</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mensagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mensagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.Id" class="text-danger">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
</main>